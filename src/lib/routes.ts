import { base } from "$app/paths";

export const routes = {
	home: `${base}`,
	featured: `${base}/featured`,
	contribute: `${base}/contribute`,
	about: `${base}/about`,
	inspiration: `${base}/inspiration`,
	resources: `${base}/resources`,
	map: `${base}/map`,
	ecopoint: `${base}/ecopoint`,
	contact: `${base}/contact`,
};
